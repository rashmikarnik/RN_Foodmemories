import * as ActionTypes from './ActionTypes';

export const recipes = (state = {
    isLoading: true,
    errMess: null,
    campsites: []
}, action) => {

//Adding Actions
switch(action.type){
    
}

}